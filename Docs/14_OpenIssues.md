# 14. 남겨둔 의도적 미해결 사항

> Atlas_CyberDepot 아키텍처 설계안 v5 — §14. 어떤 시스템을 구현하든 착수 전에 이 문서를 훑어, 해당 시스템에 걸린 미해결/미채택 사항이 있는지 확인할 것.

- **(기획 변경, 2026-07-24) RPC/멀티플레이 아키텍처 전체 — 최종 버전 삭제 대상.** 프로젝트 목적을 "AI 학습 데이터를 얻는 시뮬레이터"로 재정의하면서 실제 2인 동시 협업(멀티플레이)이 그 목적에 맞지 않는다고 판단(사용자 결정 — 포트폴리오 관점에서 기획 의도와 아키텍처 규모가 어긋나 보이는 것을 우려). 대안으로 "외부 컨트롤 클라이언트가 RPC로 시나리오를 원격 주입"하는 구조도 검토했으나 부자연스럽다고 판단해 기각 — RPC 자체는 별도 프로젝트에서 다루기로 함.
  - 실제 RPC 5종(`AFactoryPlayerController::Server_RequestPossessNPC`/`Server_ReleaseNPC`/`Server_SubmitKioskOrder`/`Server_JoinRepair`/`Server_LeaveRepair`)은 2026-07-24에 평범한 함수 호출로 전환 완료 — 기능 영향 없음(호출부 변경 없이 그대로 동작, 리슨 서버 호스트 기준 RPC 호출과 직접 호출의 실행 결과가 같기 때문). 원래 `UFUNCTION(Server, Reliable)` 선언은 각 함수 위에 주석으로 보존.
  - 리플리케이션(`Replicated` 프로퍼티, 11개 파일: `FactoryAgentBase`/`FactoryAtlasRobot`/`FactoryTransportRobot`/`FactoryNPCHuman`/`SmartFactoryManager`/`LogisticsItem`/`StorageShelf`/`IdleWaitingZone`/`HorizontalTray`/`DockingPoint`/`RepairProgressComponent`)은 이번엔 손대지 않음 — 일부 `OnRep_` 콜백이 자동 리플리케이션 발동에만 의존한다면 제거 시 실제로 기능이 바뀔 위험이 있어("서버 자신은 자기 값 변경에 대해 OnRep이 자동으로 안 불린다"는 함정, `DisplayName`은 이미 수동 호출로 우회돼 있었음) "기능에 영향 없이"라는 제약과 충돌할 수 있다고 판단해 보류. **"기능 완성 v1.0.0"로 넘어가기 직전에 각 `OnRep_`이 이미 수동 호출되고 있는지 하나씩 확인한 뒤 정리(제거)할 작업으로 명시적으로 기록.**
  - `Docs/02_Multiplayer_RPC.md` 문서 전체도 최종 버전에서 삭제 대상(문서 상단에 동일 내용 기록해둠).
- ~~`ACostZoneVolume`을 NavQueryFilter 런타임 코스트 조정 방식으로 전환했지만, 그럼에도 구현 후 `stat navigation`으로 실측은 필요하다(코스트 조정 자체도 쿼리마다 계산 비용이 있으므로).~~ → 해소(삭제): 아래 "동적 혼잡 코스트" 항목 참고 — 측정 대상 자체가 없어짐.
- ~~키오스크 전용 플레이어가 "주문 승인" 외에 NPC 디스패치 판단 같은 추가 권한을 가질지는 `02_Multiplayer_RPC.md` 확정 시 함께 정한다.~~ → 8단계에서 해소: 키오스크는 전용 플레이어가 아닌 인게임 장비로 재설계되어 전원 동일 권한(`02_Multiplayer_RPC.md`).
- ~~(8단계 신규) `FTransportTask::SourceOrderID`는 필드만 추가됐고 실제로 채워지지 않는다 — `UOutboundDispatchSubsystem::DecomposeOrder`가 아직 `PendingTransportTasks`를 채우는 로직이 없기 때문(`07_TaskAssignment.md`).~~ → 6단계 오케스트레이션 작업에서 해소: `DecomposeOrder`/`EnqueueInboundWork`가 실제로 채운다. 단, 운송 작업 취소(`TryCancelAssignmentsForOrder`가 `PendingTransportTasks`는 건드리지 않음)는 아직 미지원 — 재검토 필요.
- ~~(6단계 신규) `UOutboundDispatchSubsystem::HandoffStationAssignment`는 `ShelfOutboundZone` 교대 시 To를 `OutboundStagingTransform`으로 보내는데, Outbound 배정의 첫 다리는 원래 슬롯에서 시작해야 해서 완전히 정합하지 않는다(`07_TaskAssignment.md` 참고). 대기실(`AIdleWaitingZone`)이 아직 레벨에 배치되지 않아 실사용 안 돼 당장 영향 없음 — 대기실/교대를 실제로 테스트하는 단계에서 재검토.~~ → 해소: 대기실은 실제로는 구현 시점에 이미 배치되어 있었고, 실기 테스트에서 이 정합 문제가 그대로 드러났다(선반에 아틀라스가 이따금 2대로 보이는 증상). 소프트 핸드오프/스테이징 지점 자체를 폐기하고 "즉시 동시 교대"로 재설계해 해소(`07_TaskAssignment.md` 참고).
- ~~(6단계 신규) 아틀라스의 파트너(배송로봇) 대기 재시도(`OnWorkingTick` + `ZoneRetryIntervalSeconds`)와 배송로봇 탐색 반경(`RendezvousSearchRadius`)은 레벨 제작 후 실제 통로 길이/로봇 속도에 맞춰 튜닝이 필요한 가안이다.~~ → 6단계 후속에서 해소: 거리 기반 탐색(`RendezvousSearchRadius`) 자체를 폐기하고 `TripTaskID`(`FTransportTask::TaskID`) 정확 매칭으로 교체(`07_TaskAssignment.md`). `ZoneRetryIntervalSeconds`(재시도 간격)는 여전히 레벨 제작 후 튜닝이 필요한 가안으로 남는다.
- ~~(8단계 신규) `AFactorySpectatorPawn`의 `FactoryBoundary` 콜리전 채널은 코드/설정으로 정의만 해뒀고, 실제 경계 볼륨 배치는 레벨이 만들어지는 시점(에디터 작업)으로 미뤄져 있다.~~ → 해소(2026-07-22): 경계 볼륨 배치 + `AFactoryNPCHuman` 캡슐의 FactoryBoundary Block 응답까지 사용자가 완료.
- ~~(8단계 신규) `AFactoryNPCHuman::ReleasePossession()`은 `SpawnDefaultController()`로 AI 제어 복귀를 시도하는데... "빙의 해제 후 AI가 정상적으로 다시 순찰/작업을 재개하는지"는 아직 별도 확인 안 됨.~~ → 근본 원인까지 해소(2026-07-22, 실기 로그로 확진): 두 단계짜리 버그였다. ① `StartPatrol()`이 레벨 시작 시 BP `BeginPlay`에서 1회만 호출되는 구조라 재순찰 트리거가 없던 문제는 `ReleasePossession()`에서 `CallToOfficeExit()` 호출로 해소. ② 더 근본적으로, `AFactoryPlayerController::Server_ReleaseNPC_Implementation()`가 `ReleasePossession()`(내부에서 `SpawnDefaultController()` 호출)을 `Possess(TargetSpectatorPawn)`보다 **먼저** 호출하고 있었다 — 그 시점엔 NPC의 `Controller`가 여전히 이 PlayerController를 가리키고 있어 `APawn::SpawnDefaultController()`의 "Controller != nullptr이면 즉시 반환" 가드에 걸려 매번 조용히 no-op됐다. NPC는 AI 컨트롤러를 영영 못 받고 `Controller == nullptr`로 남았고, `StartPatrol`/`AssignMaintenance`의 `Cast<AFactoryAIController>(GetController())`가 매번 실패해 이동 요청이 나간 적이 없었다 — 상태 전환·정비 참여(도착 판정 없이 배정 시점에 즉시 처리)는 컨트롤러 없이도 동작해 겉으로는 "작동하는 것처럼" 보였다. `Possess(TargetSpectatorPawn)`을 `ReleasePossession()`보다 먼저 호출하도록 순서를 바꿔 해소.
- ~~(2026-07-22 신규, 위 조사 중 발견) `AMSmartFactoryManager`의 정비 배정 로직이 대상 NPC가 현재 플레이어에게 빙의된 상태인지 확인하지 않는다...~~ → 해소(2026-07-22): `FindNearestAvailableNPC`에 `Cast<APlayerController>(NPC->GetController())` 후보 제외 조건 추가. 원래 `FindNearestAvailableNPC` 코드에 "플레이어 빙의 여부까지 구분하는 건 8단계 멀티플레이어에서 정교화한다"는 주석이 이미 있었던 것으로 보아 8단계 작업 계획에는 있었으나 실제 구현 시 누락됐던 항목 — 이번 실기 버그 리포트로 확인 후 반영.
- ~~(8단계 신규) `IA_Interact`(신규 액션)는 코드로 생성할 수 없어 에디터에서 제작 후 `BP_FactorySpectatorPawn`/`BP_FactoryNPCHuman` 양쪽의 `InteractAction`에 할당 필요.~~ → 해소: 빙의·정비 참여/이탈·키오스크 인터렉트 전부 실기 확인됨.
- ~~(8단계 신규) `AFactoryNPCHuman`의 `OnMoveTriggered`/`OnLookTriggered`가 `IA_Move`/`IA_Look`/`IA_MouseLook` 값을 `FVector2D`(Axis2D)로 가정한다 — 실제 에셋의 Value Type이 다르면 런타임에 값이 어긋난다.~~ → 해소: 빙의 이동/시점 실기 확인됨(값이 어긋났다면 애초에 동작하지 않았을 것).
- (2026-07-22 신규) `AFactoryGameMode`의 `GameStateClass = AMSmartFactoryManager::StaticClass()`(생성자, `FactoryGameMode.cpp`)는 예전 `DefaultPawnClass` 버그와 동일한 패턴 — 실제 사용 중인 GameMode 블루프린트가 `GameStateClass`를 BP 서브클래스(`ItemPriceTable`/`VendorNames`/`MinOrderIntervalSeconds` 등이 실제로 채워진)로 오버라이드하지 않았다면, 레벨엔 값이 전혀 설정 안 된 네이티브 `AMSmartFactoryManager`가 GameState로 스폰된다. `GetUnitPrice`/`GetSellPrice`는 테이블이 비어있으면 크래시 없이 조용히 0을 반환하도록 설계돼 있어(`Docs/03_InventoryOrder.md`), 이 경우 주문 자체는 "정상 동작"하는 것처럼 보이면서 가격만 전부 0으로 처리되는 조용한 오류가 난다 — 에디터에서 실제 GameMode BP의 Class Defaults를 열어 `GameStateClass`가 무엇으로 돼 있는지 확인 필요.
- 선반 포화 해소 수단 중 "폐기 처리"의 구체 설계(전용 구역/트레이 위치, 폐기 트리거 방식, 폐기된 물품의 후속 처리)는 미정이다.
- `UFactoryDashboardWidget`(관제실)의 인게임 접근 방식 — 특정 위치에 진입해야 여는 형태인지, 아무 위치에서나 단축키로 호출 가능한 형태인지 — 는 미정이다. (9단계에서도 미구현 확정, 후순위 유지)
- (9단계 신규) `UCoopRoleHUDWidget`은 8단계에서 `EPlayerRole`이 제거되며 구현하지 않기로 확정했다(`Docs/09_Visualization.md`).
- ~~(9단계 신규) `AMyMQTTClient`가 실제 브로커에 연결할 언리얼 MQTT 플러그인이 아직 선택/설치되지 않았다~~ → 후속 갱신에서 해소: Eclipse Paho MQTT C(`MQTTAsync`)를 서드파티로 직접 연동(`Docs/11_MQTT.md`).
- ~~(MQTT 후속 신규) `Source/ThirdParty/PahoMQTT/`에 실제 라이브러리 파일이 아직 배치되지 않아 컴파일이 안 된다~~ → 해소: 사용자가 보유한 파일을 배치, 컴파일 확인됨.
- (MQTT 후속 신규) 실기 브로커가 없어 `Connect()`/구독/발행 전체가 컴파일만 확인됐고 실제 연동 테스트는 못 했다.
- (MQTT 후속 신규) `Binaries/`에는 `paho-mqtt3as.dll`/`.pdb`만 있고 OpenSSL 등 이 dll이 런타임에 의존할 수 있는 추가 라이브러리(`libssl`/`libcrypto` 등)는 확인되지 않았다 — 실행 시 로드 실패하면 별도로 구해 배치해야 할 수 있다.
- (MQTT 후속 신규) `TryPublish`는 브로커가 발행을 사후에 거부(비동기 실패 콜백)해도 자동으로 `PendingPublishQueue`에 재적재하지 않는다 — 미연결 상태의 즉시 실패만 큐잉 대상이다.
- (9단계 신규) MQTT 토픽 이름(`atlas_cyberdepot/anomaly` 등)과 JSON 필드 대소문자(`FJsonObjectConverter` 기본 변환 규칙)는 실제 브로커 연동 테스트 전까지 잠정치다 — 플러그인 연결 시 재검증 필요.
- ~~(9단계 신규) `UAgentStatusIndicatorWidget`을 에이전트 머리 위에 실제로 부착하는 `UWidgetComponent` 배치는 에디터/Content 작업으로 남아있다.~~ → v1.0.0 범위에서 제외(2026-07-22, `Docs/09_Visualization.md`) — 향후 재도입 여부도 미정.
- ~~(9단계 신규) `UMinimapWidget`/`UAgentStatusIndicatorWidget`의 실제 드로잉은 `BlueprintImplementableEvent`만 정의돼 있고, UMG 디자이너에서 위젯 블루프린트를 만들어 구현해야 한다.~~ → 위와 동일 사유로 v1.0.0 범위 제외.
- (9단계 신규, 2026-07-22) `UVendorOrderListWidget`(인바운드 주문 패널 통합, 키오스크 상호작용 연결)은 C++ 쪽(데이터/바인딩/`AFactorySpectatorPawn`의 열기·닫기·입력모드 전환)이 전부 완성됐지만, `KioskWidgetClass`에 할당할 실제 WBP(UMG 디자인)가 아직 없다 — 그 전까지는 키오스크를 인터렉트해도 화면에 아무것도 뜨지 않는다.
- `UCongestionHeatmapSubsystem`의 `UpdateIntervalSeconds`(예시 8초), `DecayRatePerUpdate`(예시 0.85)는 가안이며, 실제 플레이 규모(로봇 약 30대)에서 시각적으로 의미 있는 값인지 구현 후 튜닝이 필요하다. 유일한 소비자인 `UMinimapWidget`이 v1.0.0 범위에서 제외돼(위 항목 참고) 이 튜닝도 함께 후순위.
- `AIdleWaitingZone`의 `RestDecayIntervalSeconds`(예시 10초)·`RestDecayAmountPerInterval`(예시 10)·`FullyRestedThresholdRatio`(예시 0.2)와 각 로봇의 `MaintenanceThreshold`는 상호 균형이 중요하다. 실제 로봇 운용 규모에서 플레이테스트 후 튜닝 필요.
- ~~`HandoffStationAssignment`를 소프트 핸드오프로 바꿨지만, To가 스테이징 지점에서 대기하는 동안 제3의 아틀라스가 해당 스테이징 지점을 우연히 지나가는 경합은 발생하지 않는지(스테이징 지점은 점유 예약 대상이 아니므로) 구현 후 검증 필요.~~ → 해소: 소프트 핸드오프/스테이징 지점 자체를 폐기(위 항목 참고) — 경합 우려의 전제가 되는 대기 구간 자체가 없어짐.
- ~~(핸드오프 재설계 신규) `AStorageShelf::InboundStagingMarker`/`OutboundStagingMarker`(및 `GetInboundStagingLocation`/`GetOutboundStagingLocation`)는 이제 코드 어디에서도 참조되지 않는 정리 후보다. 레벨에 이미 BP 컴포넌트로 배치돼 있을 수 있어 임의로 C++ 클래스를 삭제하지 않았다 — 삭제 여부는 사용자 확인 후 별도 진행.~~ → 삭제로 해소: 사용자가 레벨의 `AStorageShelf`에 해당 컴포넌트가 배치돼 있지 않음을 확인, 컴포넌트 클래스·조회 함수 전부 삭제.
- ~~(10단계 신규) `UBenchmarkHarnessSubsystem::RecordPerfSample`의 `NavigationTickTimeMs`는 대응하는 공개 전역 카운터가 없어 항상 0 — 실측은 `stat navigation` 수동 확인에 계속 의존한다.~~ → 삭제로 해소(2026-07-24): 엔진 소스 확인 결과 신뢰할 수 있게 고칠 방법 자체가 없다고 판단(내비게이션 계측치가 여러 하위 항목으로 쪼개져 있어 공개 API가 없고, 실제 병목의 대부분일 군중회피 연산은 엔진이 자체적으로 돌려 자체 계측으로도 못 잡음) — 항상 0인 죽은 필드로 남기지 않고 `FPerfSample`/CSV/평균 계산에서 전부 제거. 자세한 내용은 `Docs/10_Benchmark_Replay.md` 참고.
- ~~(10단계 신규) `RunScalingComparison`/`StartForcedDeadlockDemo`는 레벨/내비메시가 없는 기획단계 특성상 코드 구조만 완성됐고 실기 계측·시연 테스트는 못 했다. 레벨이 만들어지면 `AgentClassToSpawn`/`SpawnOrigin` 등 밸런싱 값을 실제 레벨에 맞게 조정하고 재검증 필요.~~ → `RunScalingComparison`은 해소(2026-07-23): 레벨에 아틀라스12+배송13+NPC3 배치 완료, `UBenchmarkSettings::AgentClassToSpawn`에 실제 클래스 지정 후 `Benchmark.RunScalingComparison 20 2` 등으로 실기 계측 확인됨(TickTime+11.9%/Memory+0.8%/GPU+4.1%, sub-linear 스케일링 — 믿을 만한 결과로 판단됨, 2회 재현). 결과 확인은 콘솔 로그(`PrintScalingReport`)/CSV(`ExportPerfReport`)로 충분하고 시뮬레이터 구동 중 인게임 UI로 표기할 필요는 없다고 판단(사용자 결정, 2026-07-23) — 별도 UI 작업 없음. `StartForcedDeadlockDemo`는 2026-07-24에 삭제됨(아래 `Code:001` 존치 조건부 항목 참고).
- ~~(10단계 신규) `UReplayPlaybackSubsystem`이 방출하는 `FOnPlaybackFrame` 델리게이트를 실제 화면에 어떻게 시각화(고스트 액터 스폰 등)할지는 미정 — 소비자 측 설계가 필요하다.~~ → 해소(2026-07-23): `UReplayVisualizationSubsystem`+`AReplayGhostActor`(+`UReplaySettings`의 타입별 고스트 클래스)로 구현, 재생 트리거(`AFactorySpectatorPawn::ToggleReplayAction`)까지 실기 확인됨(`Docs/02_Multiplayer_RPC.md` 참고).
- ~~(10단계 신규, 2026-07-23) `UBenchmarkHarnessSubsystem`은 여전히 호출부가 전혀 없다 — `RunScalingComparison`/`StartForcedDeadlockDemo`/`ExportPerfReport` 셋 다 정의만 있고 프로젝트 어디에서도 호출되지 않는다(사용자가 "무조건 도입해야 한다"고 확정한 시스템이라 위 33번 항목과 별개로 명시). 디버그 키/콘솔 커맨드 등 실행 진입점 설계가 필요.~~ → 해소(2026-07-23): `Benchmark.RunScalingComparison`/`Benchmark.PrintScalingReport`/`Benchmark.ExportPerfReport`/`Benchmark.StartForcedDeadlockDemo` 콘솔 커맨드로 구현(`Initialize()`에서 클라이언트 월드는 등록 스킵, `Deinitialize()`에서 해제). 위 33번(실기 계측 미검증)은 레벨 부재로 여전히 별개로 열려 있음.
- ~~(10단계 신규, 2026-07-23) `UReplayPlaybackSubsystem::Pause()`/`SeekToTime()`/`PlaybackSpeed`는 구현은 돼 있지만 호출부가 전혀 없다 — 지금 재생 UX는 `AFactorySpectatorPawn::ToggleReplayAction`(재생/정지 토글)뿐이라 일시정지·탐색(스크러빙)·배속 조절을 플레이어가 쓸 방법이 없다.~~ → 해소(2026-07-23): `UReplayControlWidget`(온스크린 스크럽바 UI, 사용자 선택)+`AFactorySpectatorPawn::ToggleReplayControlsAction`으로 구현. 구현 과정에서 `SeekToTime`이 탐색 시점 프레임을 재방출하지 않던 버그, `OnToggleReplayTriggered`가 `IsPlaying()`만으로 정지/재시작을 분기해 일시정지 상태에서 오작동하던 버그도 함께 발견해 수정(`Docs/10_Benchmark_Replay.md` 참고). 되감기(실시간 역재생)는 사용자 결정으로 범위 밖 — 구간 탐색(`SeekToTime`)만 지원. 스크럽바/재생·일시정지/배속 버튼 전부 실기 확인됨(2026-07-23).
- (10단계, 확정 사항 — 미구현 아님) `UReplayRecorderSubsystem`/`UTrainingDataRecorderSubsystem` 둘 다 `StopRecording()`을 거는 수동 트리거가 없고 세션 종료(`Deinitialize`, 월드 종료)에만 의존한다. 사용자가 명시적으로 "추가하지 않을 것"으로 확정(2026-07-23) — 더 이상 열린 항목이 아니며 다시 제안하지 말 것.
- ~~(10단계 신규, 2026-07-23) `FTrainingLogEntry`는 초기 요청("모든 폰과 액터의 각종 정보, current state에서 변경되는 세부 정보 등 아주 자세한 정보")보다 좁게 구현됐다 — 현재 `CurrentState`/`Location`/`MoveDestination`/`SelectedWaypointName`뿐이고, 소지 아이템·정비 진행도·현재 배정 ID 등 상태 세부값은 담기지 않는다. AI 학습 데이터로 실제 활용하기 전에 필드 확장 필요 여부 재검토 요망.~~ → 해소(2026-07-23): `bIsCarryingItem`/`CarriedItemType`/`RepairProgress`/`CurrentAssignmentID`/`OperationCount`/`bIsPlayerControlled` 6개 필드 추가(SchemaVersion 1→2), `Docs/01_EventBus_DataPipeline.md` 참고. 단, `CurrentAssignmentID`/`OperationCount`는 원본 필드가 리플리케이트되지 않아 클라이언트 인스턴스가 발행한 로그에서는 부정확할 수 있음(서버 로그만 신뢰) — 필요하면 후속으로 리플리케이션 추가 여부 재검토.
- ~~(10단계 신규, 2026-07-23) `Benchmark.StartForcedDeadlockDemo`가 발행하는 `Code:001` 이상징후를 실제로 소비하던 곳은 `UCongestionHeatmapSubsystem`(셀 저장만) → `UMinimapWidget::BP_OnAnomalyMarkerAdded`(실제 렌더링) 경로가 유일했는데, 사용자가 초기 기획 대비 축소된 범위로 미니맵 자체를 제외하기로 확정(2026-07-23) — 지금은 발행해도 관측 가능한 소비자가 전혀 없다. 상태 변화 알림(교착상태 등) 기능은 "구현 계획 없음, 나중에 생길지도 불확실"로 확정. 그래도 `StartForcedDeadlockDemo`는 존치하기로 결정(사용자 승인) — 이유: (1) 격리된 함수라 유지 비용이 사실상 0, (2) 나중에 알림/이상탐지 UI가 조금이라도 생기면 실제 교착 없이 즉시 그 파이프라인을 검증할 수 있는 유일한 수단, (3) 의존하는 `FAnomalyEvent`/`Code:001` 자체는 `Docs/01_EventBus_DataPipeline.md`의 핵심 이벤트 버스 스키마라 이 커맨드와 무관하게 계속 존재함. **존치 조건부**: "기능 완성 v1.0.0" 시점까지 이상징후를 실제로 소비하는 알림/UI가 하나도 안 생기면, 그때는 `StartForcedDeadlockDemo`(+콘솔 커맨드, `FStressTestParams`)를 삭제 대상으로 재검토할 것 — v1.0.0 안정화 점검 시 먼저 후보로 꺼내 사용자에게 제안.~~ → 삭제로 해소(2026-07-24, 조건부 대기 없이 즉시 재검토·삭제): 재검토 중 위 존치 사유 (2)가 실제로는 근거가 약하다는 게 드러남 — `StartForcedDeadlockDemo`가 쓰는 `OnAnomalyPublished` 채널은 `UTrainingDataRecorderSubsystem`(`FTrainingLogEntry`만 구독)과 완전히 분리돼 있어 AI 학습 데이터에 애초에 전혀 기여하지 않았고, 설령 연결됐어도 `Velocity`/`TargetLocation` 등이 전부 기본값인 가짜 이벤트라 오히려 라벨 오염(노이즈)이 됐을 것. 함수/타이머/`FStressTestParams`/콘솔 커맨드 전부 삭제.
- ~~(5단계 후속 신규) `UInventoryOrderSubsystem::TryPlaceOrder`가 Inbound 트레이에 물품을 올릴 때, 트레이가 이미 점유 중이면 이번 호출에서는 물리적으로 안 올리고 넘어간다 — `Quantity`가 1보다 큰 주문의 나머지 수량을 트레이가 빌 때마다 이어서 자동으로 흘려보내는 대기열이 없다.~~ → 해소: `PendingInboundQuantities`(품목별 누적) + `AFactoryAtlasRobot::TransferItem`이 Inbound 트레이를 비운 직후 호출하는 `OnInboundTrayCleared` → `TryDrainInboundBacklog`로, 트레이가 빌 때마다 대기 수량을 1개씩 이어서 흘려보낸다.
- ~~(7단계 후속 신규) 자연 발생 고장에서 복구된 로봇은 `SetState(Idle)`만 될 뿐, 보존된 배정/트립을 실제로 이어서 재개하는 로직이 없다.~~ → 실기 테스트로 확인된 연쇄 정지(적재 선반에서 아틀라스 고장 시 대기 중인 배송로봇도 영구 정지, 그 배송로봇은 어떤 디스패치 트리거로도 재개 불가)를 계기로 해소: `AFactoryAtlasRobot`/`AFactoryTransportRobot::ResumeAfterRepair()`가 `CurrentAssignment`/`CurrentTask`가 유효하면 `Idle` 대신 `Working`으로 복귀시킨다. 아틀라스는 기존 `OnWorkingTick`의 `ZoneRetryIntervalSeconds` 재시도 루프(`ContinueShelfAssignment`/`ContinueTrayAssignment`, 원래부터 재진입 안전하게 설계됨)가 자동으로 이어받고, 배송로봇은 능동 재시도가 없어도 `Working`으로만 돌아가면 아틀라스 쪽 `FindWaitingTransportRobot`(`CurrentState==Working` 요구)이 다시 찾아낸다.
- (금액 산정 시스템 신규) `UInventoryOrderSubsystem::ReorderCooldownSeconds`(전역 단일 재주문 대기시간)는 "물류센터에 들어오는 차량과 연계"할 예정인 임시 제약이다 — 실제 차량 도착/배송 시스템이 설계되면 고정 시간 대신 그 시스템이 재주문 가능 시점을 결정하도록 대체해야 한다.
- (금액 산정 시스템 신규, 2026-07-22) 인바운드 주문 패널(`WBP_VendorOrderList`)의 재주문 쿨다운(현재 임시 5초)에 대한 클라이언트 측 시각 피드백(버튼 비활성화, 카운트다운 텍스트, 이펙트 등)이 없다 — 쿨다운 중 "주문하기"를 눌러도 서버가 조용히 거부할 뿐 화면엔 아무 반응이 없다. 의도된 단순화(`Docs/09_Visualization.md` 참고)이나, 실제 디자인 작업 시 반드시 다시 꺼내 UI를 붙여야 한다.
- ~~(금액 산정 시스템 신규) `UVendorOrderListWidget::AcceptOrder`는 기존 `Server_SubmitKioskOrder` RPC를 재사용하는데, 이 RPC는 `SourceKiosk`와의 물리적 거리(`KioskInteractRadius`)를 검사한다.~~ → 완전 해소(2026-07-22, 사용자 지시): 거리 재검증 개념 자체를 삭제. `KioskInteractRadius` 프로퍼티도 함께 제거했다 — 위젯을 여는 시점의 사거리(`InteractTraceDistance`/`KioskInteractTraceDistance`)만이 유일한 게이트이므로 서버 쪽 재검증도, 두 사거리 값의 불일치 문제도 더 이상 없다.
- (2026-07-22 신규) 키오스크 상호작용을 F(인터렉트)에서 좌클릭(`IA_Click`)으로 옮기면서 `AFactorySpectatorPawn`/`AFactoryNPCHuman` 양쪽에 새 Enhanced Input 액션이 필요해졌다 — `IA_Interact`와 마찬가지로 코드로 생성 불가, 에디터에서 제작 후 양쪽 BP의 `ClickAction`에 할당 필요(마우스 왼쪽 버튼 매핑). `AFactoryNPCHuman`은 추가로 `KioskWidgetClass`(F/좌클릭 재설계와 함께 신설)도 할당해야 한다.
- (2026-07-22 신규) 키오스크 위젯이 열린 동안 이동/시점을 막는 데 `PlayerController::SetIgnoreMoveInput`/`SetIgnoreLookInput`를 썼다. `AFactoryNPCHuman`은 자체 `OnMoveTriggered`/`OnLookTriggered` 가드로 이중 보장되지만, `AFactorySpectatorPawn`의 자유비행 이동은 `ASpectatorPawn` 내장 레거시 축 입력이라 이 두 함수가 실제로 그 입력까지 막아주는지 코드만으로는 100% 확신할 수 없다 — 실기 테스트로 관전자 상태에서도 위젯이 열린 동안 이동이 완전히 멈추는지 확인 필요.
- (2026-07-22 신규) 빙의 상태에서도 관전자 상태와 동일하게 키오스크 상호작용이 되도록 `AFactoryNPCHuman`에 `FindKioskInFrontOfCamera`/`OpenKioskWidget`/`CloseKioskWidget`를 신규 구현했다 — 관전자 쪽은 기존에 검증됐지만, 빙의 중 좌클릭으로 키오스크를 여닫는 동작 자체는 아직 실기 확인 전이다.
- ~~(회피 시스템 재설계 논의 중 신규) `ACostZoneVolume`/`AFactoryAIController::ApplyDynamicCongestionCost`(동적 혼잡 코스트) — 삭제 후보. 코스트 오버라이드가 우리 웨이포인트 그래프가 아니라 엔진 NavMesh 이동(`MoveTo`)의 NavArea 클래스 전체에 적용되는 방식이라, 짧고 대안 경로가 거의 없는 웨이포인트 홉/FinalHop 구간에서는 실효가 미미하다. 아틀라스는 애초에 이 시스템을 쓰지 않는다(`AFactoryTransportRobot`만 `OnBlockedTick`/`OnUnblocked`를 오버라이드). 자유이동 구간을 최소화하는 방향(회피 재설계)과 태생적으로 맞지 않음 — 지금 당장 제거하지 않고, 재설계 완료 후 재검토.~~ → 삭제로 해소: 회피 재설계(그래프 구간/FinalHop 이중 체계) 완료 후 재검토, 삭제 확정. NPC 3명뿐이라 congestion 시나리오 자체가 드물고, 유일한 Blocker 등록 주체였던 `AFactoryTransportRobot`의 정지 상태는 이미 §8-B 예약/Pause/재탐색이 처리하며, 로봇-NPC 충돌 회피는 RVO + 안전거리 감지(`Cast<AFactoryAgentBase>` 기반이라 `AFactoryNPCHuman`도 대상)가 이미 이중으로 커버하고 있어 실효 손실이 없다고 판단(`Docs/08_Navigation.md` §8-A 갱신).

## 검토 후 미채택 사항 (근거 포함)

- **예약 락 타임아웃/리스(lease) 패턴**: 로봇이 고장나도 슬롯/거점 예약을 유지한 채 수리 후 재개하는 것이 설계 원칙(`00_DesignPrinciples.md`)이고, FullRepair는 유한 시간 내 항상 완료되므로 영구 락(zombie lock) 위험이 구조적으로 없다. 타임아웃으로 강제 해제하면 오히려 "재개" 원칙이 깨진다. 미채택.
- **고장 위험 로봇을 시스템이 강제로 대기실까지 Navigate시키는 안**: `EvaluateRotationOrContinue` 기반 로테이션/핸드오프가 이동 직전·`TransferItem` 직전이라는 자연스러운 작업 경계에서만 판단하므로, 임무 도중 시스템이 로봇을 강제 인터럽트하는 것보다 우아하고 원칙에도 부합한다. 미채택.
- **배치 정비 중단 시 `Paused` 상태 별도 도입**: `RepairProgress`가 로봇 컴포넌트에 저장되고 `ActiveRepairers`가 0이 되어도 값은 보존되므로(진행 속도만 0), 중단 후 `BatchMaintenanceTargetSet`을 초기화하고 재트리거 시 새 스냅샷을 뜨는 현재 방식으로도 진행 상황 유실이 없다. 별도 상태머신을 추가하면 오버엔지니어링. 미채택.
